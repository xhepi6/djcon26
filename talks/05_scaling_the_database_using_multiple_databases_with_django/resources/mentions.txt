 - scaling horizontally
 - sharding
 - multiple databases: replica + master
 - how do we conenct django to it?
 - django -> load_balancer -> replicas
 - how to tell django to use a certain db?
 - MyModel.objects.using('replica').all() -> redundant
 - class MyDBRouter:
 - db_for_read
 - return "replica"
 - db_for_write
 - return "default"
 - allow_relations
 - allow migrate
 - if db is replica -> False
 - set DATABASE_ROTUER to the MyBRouter
 - challenges
 - Transactions:
 - with transaction.atomic():
 - user.objects.create(username=;jake;)
 - print(user.all().count()) ->this is 0
 - fix:
 - modify get_read to check if we are in autocommit
 - Django itself
 - User.objects.get_or_create -> goes to primary
 - fix:
 - create replica_aware_get_or_create
 - try to get the qs.get(), if fails create it with get or create
 - what is mirroring for testing?
 - GenericRelation uses read connection for writes -> BUG #36389